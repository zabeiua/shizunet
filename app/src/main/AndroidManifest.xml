<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <queries>
        <package android:name="moe.shizuku.privileged.api" />
        <provider android:authorities="moe.shizuku.privileged.api.shizuku" />
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:label="ShizuNet"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
        
        <!-- Multi-process configuration for Shizuku dynamic binder routing -->
        <meta-data
            android:name="rikka.shizuku.multiprocess"
            android:value="false" />

        <!-- Explicit declaration of ShizukuProvider to bypass merge or initialization issues -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="com.example.shizukuproxy.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
